services:
  # PostgreSQL (운영환경에서 사용)
  # 아래 postgres 서비스와 app의 환경변수 주석을 해제하세요
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: kanban-postgres
  #   environment:
  #     POSTGRES_DB: kanban
  #     POSTGRES_USER: kanban
  #     POSTGRES_PASSWORD: kanban
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   restart: unless-stopped

  app:
    image: ghcr.io/world-goes-to-hell/ysk-kanban:latest
    container_name: kanban-board
    ports:
      - "8080:8080"
    volumes:
      - /home/junu/docker_data/kanbanboard/data:/app/data
      - /home/junu/docker_data/kanbanboard/uploads:/app/uploads
    environment:
      - TZ=Asia/Seoul
      - JAVA_OPTS=-Duser.timezone=Asia/Seoul
      # 업무보고 AI 양식 추출 (사내 OpenWebUI). 미설정 시 해당 기능만 비활성화됨.
      # 값은 커밋하지 말고 호스트 환경변수 또는 .env 로 주입.
      - OPENWEBUI_BASE_URL=${OPENWEBUI_BASE_URL:-}
      - OPENWEBUI_API_KEY=${OPENWEBUI_API_KEY:-}
      - OPENWEBUI_MODEL=${OPENWEBUI_MODEL:-gpt-oss:120b}
      # PostgreSQL 사용 시 아래 주석 해제
      # - SPRING_PROFILES_ACTIVE=prod
      # - DB_URL=jdbc:postgresql://postgres:5432/kanban
      # - DB_USERNAME=kanban
      # - DB_PASSWORD=kanban
    # depends_on:
    #   - postgres
    restart: unless-stopped

# volumes:
#   postgres_data:
